query AllMatters {
  matters(orderBy: UPDATED_AT_ASC) {
    nodes {
      id
      primaryContact {
        id
        name
        email
      }
      matterTemplate {
        id
        name
        category
      }
      name
      createdAt
      updatedAt
    }
  }
}
