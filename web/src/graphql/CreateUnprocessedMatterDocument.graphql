mutation CreateUnprocessedMatterDocument(
  $matterId: UUID!
  $uploadDocumentUrl: String!
  $documentTemplateId: UUID!
) {
  createUnprocessedMatterDocument(
    input: { matterId: $matterId, uploadDocumentUrl: $uploadDocumentUrl }
  ) {
    matterDocument {
      id
    }
  }
}
