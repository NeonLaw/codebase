mutation CreateDocumentTemplate($name: String!, $description: String!) {
  createDocumentTemplate(
    input: { documentTemplate: { name: $name, description: $description } }
  ) {
    documentTemplate {
      id
      name
      description
    }
  }
}
