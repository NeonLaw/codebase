query MatterById($id: UUID!) {
  matterById(id: $id) {
    id
    name
    personByPrimaryContactId {
      id
      name
    }
    matterTemplateByMatterTemplateId {
      id
      name
      category
    }
  }
}
